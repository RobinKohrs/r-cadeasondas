}
raw_data = raw_data$result
# formt raw mapview data
formatted_data = rondas::format_raw_mapview_data(raw_data)
# regions data
regions_data[[i]] = formatted_data
}
# filter out nas ----------------------------------------------------------
regions_data = regions_data[!is.na(regions_data)]
regions_data
# bind --------------------------------------------------------------------
all = bind_rows(regions_data)
library(lubridate)
# create path -------------------------------------------------------------
time_id = format(Sys.time(), "%Y_%m_%d")
time_id
Sys.time() %>% lubridate::hour()
h = Sys.time() %>% lubridate::hour()
if(h > 13){
time_id = glue("{time_id}_m")
}else{
time_id = glue("{time_id}_a")
}
time_id
# create path -------------------------------------------------------------
time_id = format(Sys.time(), "%Y_%m_%d")
h = Sys.time() %>% lubridate::hour()
if(h > 13){
time_id = glue("{time_id}_a")
}else{
time_id = glue("{time_id}_m")
}
time_id
op_rda
# create path -------------------------------------------------------------
time_id = format(Sys.time(), "%Y_%m_%d")
h = Sys.time() %>% lubridate::hour()
if(h > 13){
time_id = glue("{time_id}_a")
}else{
time_id = glue("{time_id}_m")
}
op_rda = glue("~/data/surfdata/{time_id}.Rda")
op_rda
save(all, file=op_rda)
load(op_rda)
a = load(op_rda)
a
all %>% glimpse
all %>% select(where(is.list))
all %>% select(where(is.list)) %>% glimpse
# bind --------------------------------------------------------------------
all = bind_rows(regions_data) %>%
select(-where(is.list))
all
# create path -------------------------------------------------------------
time_id = format(Sys.time(), "%Y_%m_%d")
h = Sys.time() %>% lubridate::hour()
if(h > 13){
time_id = glue("{time_id}_a")
}else{
time_id = glue("{time_id}_m")
}
if(!dir.exists(dirname(op))){
dir.create(dirname(op), recursive = T)
}
write_csv(all, op)
# create path -------------------------------------------------------------
time_id = format(Sys.time(), "%Y_%m_%d")
h = Sys.time() %>% lubridate::hour()
if(h > 13){
time_id = glue("{time_id}_a")
}else{
time_id = glue("{time_id}_m")
}
op = glue("~/data/surfdata/{time_id}.csv")
if(file.exists(op)){
unlink(op)
}
if(!dir.exists(dirname(op))){
dir.create(dirname(op), recursive = T)
}
write_csv(all, op)
library(tidyverse)
library(here)
library(glue)
library(sf)
library(rajudas)
library(jsonlite)
library(lubridate)
# get the cells -----------------------------------------------------------
cells = rondas::get_cells(4*4*4)
# safe version of queriying data ------------------------------------------
safe_fromJSON = purrr::safely(fromJSON)
# get data ----------------------------------------------------------------
regions_data = vector("list", length(length(cells)))
for(i in seq_along(cells)){
print(i)
# cell
cell = cells[[i]]
#url
url = glue("https://services.surfline.com/kbyg/mapview?south={cell$south}&west={cell$west}&north={cell$north}&east={cell$east}")
# raw data
raw_data = safe_fromJSON(url)
if(!is.null(raw_data$error)){
next
}
raw_data = raw_data$result
# formt raw mapview data
formatted_data = rondas::format_raw_mapview_data(raw_data)
# regions data
regions_data[[i]] = formatted_data
}
# filter out nas ----------------------------------------------------------
regions_data = regions_data[!is.na(regions_data)]
regions_data %>% length
# bind --------------------------------------------------------------------
all = bind_rows(regions_data) %>%
select(-where(is.list))
# create path -------------------------------------------------------------
time_id = format(Sys.time(), "%Y_%m_%d")
h = Sys.time() %>% lubridate::hour()
if(h > 13){
time_id = glue("{time_id}_a")
}else{
time_id = glue("{time_id}_m")
}
write_csv(all, "~/Desktop/all.csv")
saveRDS(all, "~/Desktop/all.Rds")
dim(al)
dim(all)
a = readRDS("~/Desktop/all.Rds")
a
# create path -------------------------------------------------------------
time_id = format(Sys.time(), "%Y_%m_%d")
h = Sys.time() %>% lubridate::hour()
if(h > 13){
time_id = glue("{time_id}_a")
}else{
time_id = glue("{time_id}_m")
}
time_id
cronR::cron_rstudioaddin()
d = readRDS("~/data/surfdata/2023_11_17_m.Rds")
d %>% filter(str_detect(name, "Tonel"))
d %>% filter(str_detect(name, "Tonel")) %>% View
d %>% filter(str_detect(name, "Tonel")) %>% glimpse
library(tidyverse)
library(here)
library(glue)
library(sf)
library(rajudas)
library(jsonlite)
library(lubridate)
# get the cells -----------------------------------------------------------
cells = rondas::get_cells(4*4*4)
# safe version of queriying data ------------------------------------------
safe_fromJSON = purrr::safely(fromJSON)
# get data ----------------------------------------------------------------
regions_data = vector("list", length(length(cells)))
for(i in seq_along(cells)){
print(i)
# cell
cell = cells[[i]]
#url
url = glue("https://services.surfline.com/kbyg/mapview?south={cell$south}&west={cell$west}&north={cell$north}&east={cell$east}")
# raw data
raw_data = safe_fromJSON(url)
if(!is.null(raw_data$error)){
next
}
raw_data = raw_data$result
# formt raw mapview data
formatted_data = rondas::format_raw_mapview_data(raw_data)
# regions data
regions_data[[i]] = formatted_data
}
# filter out nas ----------------------------------------------------------
regions_data = regions_data[!is.na(regions_data)]
# bind --------------------------------------------------------------------
all = bind_rows(regions_data) %>%
select(-where(is.list))
# create path -------------------------------------------------------------
time_id = format(Sys.time(), "%Y_%m_%d")
h = Sys.time() %>% lubridate::hour()
if(h > 13){
time_id = glue("{time_id}_a")
}else{
time_id = glue("{time_id}_m")
}
time_id
op = glue("~/data/surfdata/per_day/{time_id}.Rds")
if(file.exists(op)){
unlink(op)
}
if(!dir.exists(dirname(op))){
dir.create(dirname(op), recursive = T)
}
op
# create path -------------------------------------------------------------
time_id = format(Sys.time(), "%Y_%m_%d_%h")
time_id
# create path -------------------------------------------------------------
time_id = format(Sys.time(), "%Y_%m_%d_%H")
time_id
# format data per spot ----------------------------------------------------
per_spot_files = dir("~/data/surfdata/per_spot/", "*")
per_spot_files
length(per_spot_files) > 0
dir_name = "~/data/surfdata/per_day/"
last_file = op
op
op = glue("~/data/surfdata/per_day/{time_id}.Rds")
last_file = op
last_file
files = list(last_file)
files
j = 1
f = files[j]
# date
bn = basename(f)
f = files[j]
f
f = files[[j]]
f
# date
bn = basename(f)
bn
date_str = str_extract(bn, "\\d+_\\d+_\\d+_\\d+")
date_str
cronR::cron_rstudioaddin()
devtools::install()
devtools::install()
devtools::install()
dir_name = "~/data/surfdata/per_day/"
last_file = NULL
# files
if(!is.null(last_file)){
files = list(last_file)
}else{
files = dir(dir_name, ".*\\.Rds", full.names = T)
output_files = dir("~/data/surfdata/per_spot/", "*", full.names = T)
unlink(output_files)
}
files
j = 1
f = files[[j]]
# date
bn = basename(f)
date_str = str_extract(bn, "\\d+_\\d+_\\d+_\\d+")
# data
d = readRDS(f)
per_id = d %>%
split(.$`_id`)
library(tidyverse)
j
f = files[[j]]
# date
bn = basename(f)
date_str = str_extract(bn, "\\d+_\\d+_\\d+_\\d+")
# data
d = readRDS(f)
per_id = d %>%
split(.$`_id`)
i = 1
cat(glue("{j}: {i}\r"))
spot = per_id[[i]]
name = spot$name[[1]] %>% janitor::make_clean_names()
path_spot = glue("~/data/surfdata/per_spot/{name}.csv")
spot[["date"]] = date_str
spot %>% glimpse
d_spot = spot %>%
select(date, lat, lon, name, conditions__value, rating__key, rating__value, matches("^surf|waveHeight|wind__|waterTemp|swells__|weather__"))
d_spot %>% glimpse
devtools::install()
cronR::cron_rstudioaddin()
cronR::cron_rstudioaddin()
devtools::is.package()
devtools::install()
dir_csvs = "~/data/surfdata/per_spot/"
dir_daily_spot_clean="~/data/surfdata/per_spot_clean/"
# data for each spot
csvs_each_spot = dir(dir_csvs, ".*\\.csv", full.names = T)
csvs_each_spot %>% length
library(tidyverse)
# data for each spot
csvs_each_spot = dir(dir_csvs, ".*\\.csv", full.names = T)
csvs_each_spot
i = 1
print(i)
# file to spot data
f = csvs_each_spot[[i]]
d
f
# raw data
d = data.table::fread(f)
d
# output file for that spot
op = glue("{dir_daily_spot_clean}/{basename(f)}")
# only use the swell with the lowest "swell__event" ?!
d_one_swell_per_time = d %>%
group_by(time) %>%
filter(case_when(
all(is.na(swells__event)) ~ swells__swell_nr == 1,
.default = swells__event == min(swells__event, na.rm = T)
))
library(glue)
# output file for that spot
op = glue("{dir_daily_spot_clean}/{basename(f)}")
# only use the swell with the lowest "swell__event" ?!
d_one_swell_per_time = d %>%
group_by(time) %>%
filter(case_when(
all(is.na(swells__event)) ~ swells__swell_nr == 1,
.default = swells__event == min(swells__event, na.rm = T)
))
d_one_swell_per_time
d_one_swell_per_time %>% View
# for each day get the average rating, the average wave height and the average
# water temperature
avg_per_day_spot = d_one_swell_per_time %>%
mutate(date = substr(time, 1, 10),
date = as.Date(date, "%Y_%m_%d")) %>%
group_by(date) %>%
summarise(
lat = first(lat),
lon = first(lon),
name = as.character(first(name)),
daily_mean_swell_rating = mean(rating__value, na.rm=T),
daily_mean_wave_height_min = mean(surf__min, na.rm=T),
daily_mean_wave_height_max = mean(surf__max, na.rm=T),
daily_mean_water_temperature_max = mean(waterTemp__max, na.rm=T),
daily_mean_water_temperature_min = mean(waterTemp__min, na.rm=T),
daily_mean_water_temperature_max = mean(waterTemp__max, na.rm=T),
weather_per_timestamp = paste0(weather__condition, collapse = ","),
wind_per_timestamp = paste0(wind__directionType, collapse = ",")
)
avg_per_day_spot
# for each day get the average rating, the average wave height and the average
# water temperature
avg_per_day_spot = d_one_swell_per_time %>%
mutate(date = substr(time, 1, 10),
date = as.Date(date, "%Y_%m_%d")) %>%
group_by(date) %>%
summarise(
lat = first(lat),
lon = first(lon),
name = as.character(first(name)),
daily_mean_swell_rating = mean(rating__value, na.rm=T),
daily_mean_wave_height_min = mean(surf__min, na.rm=T),
daily_mean_wave_height_max = mean(surf__max, na.rm=T),
daily_mean_water_temperature_max = mean(waterTemp__max, na.rm=T),
daily_mean_water_temperature_min = mean(waterTemp__min, na.rm=T),
daily_mean_water_temperature_max = mean(waterTemp__max, na.rm=T),
weather_per_timestamp = paste0(weather__condition, collapse = ","),
wind_per_timestamp = paste0(wind__directionType, collapse = ","),
n_samples_per_day = n()
)
avg_per_day_spot
avg_per_day_spot %>% glimpse
library(tidyverse)
library(here)
library(glue)
library(sf)
library(rajudas)
library(jsonlite)
library(lubridate)
# dirs for the final data -------------------------------------------------
dirs =
list(
raw_data = c("~/data/surfdata/per_day"),
per_spot_raw = c("~/data/surfdata/per_spot"),
per_spot_clean = c("~/data/surfdata/per_spot_clean")
)
dirs
walk(dirs, function(d) {
if (!dir.exists(d)) {
dir.create(d, recursive = T)
}
})
# get the cells -----------------------------------------------------------
cells = rondas::get_cells(4*4*4)
cells
hawaii = c(y=19.8,x=-155.8)
cells
cells
c = cells[[1]]
c
hawaii = c(y=19.8,x=-155.8)
hawaii = list(y=19.8,x=-155.8)
map(cells, function(c){
return(hawaii$y > c$south &&
hawaii$y < c$north &&
hawaii$x > c$west &&
hawaii$x < c$east)
})
c
c = cells[3]
x
c
c = cells[[4]]
c
hawaii$y > c$south &&
hawaii$y < c$north &&
hawaii$x > c$west &&
hawaii$x < c$east
hawaii$y > c$south
hawaii$y < c$north
hawaii$x > c$west
y
n_quadrants = 64
is_power = isPower(n_quadrants)
if(!is_power){
stop("Must be power of 4...")
}
cells = build_cells(n_quadrants)
devtools::load_all()
is_power = isPower(n_quadrants)
if(!is_power){
stop("Must be power of 4...")
}
cells = build_cells(n_quadrants)
cells
n = 16
# how many vertical sections? -----------------
x_spacing = 360 / sqrt(n)
y_spacing = 180 / sqrt(n)
columns = vector("list", length = n)
x = 1
y = 1
column = vector("list", length = sqrt(n))
for (y in 1:sqrt(n)) {
north = 90 - (y - 1) * y_spacing
south = 90 - y * y_spacing
east =  (-180) + ((x - 1) * x_spacing)
west = -180 + x * x_spacing
cell = list(
north = north,
east = east,
south = south,
west = west
)
index = ((x-1) * sqrt(n)) + y
columns[[index]] = cell
}
cell
north = 90 - (y - 1) * y_spacing
north
south
y_spacing
y
y = 1
north = 90 - (y - 1) * y_spacing
north
south = 90 - y * y_spacing
south
east =  (-180) + ((x - 1) * x_spacing)
east
west = -180 + x * x_spacing
west
devtools::install()
library(tidyverse)
library(here)
library(glue)
library(sf)
library(rajudas)
library(jsonlite)
library(lubridate)
# dirs for the final data -------------------------------------------------
dirs =
list(
raw_data = c("~/data/surfdata/per_day"),
per_spot_raw = c("~/data/surfdata/per_spot"),
per_spot_clean = c("~/data/surfdata/per_spot_clean")
)
walk(dirs, function(d) {
if (!dir.exists(d)) {
dir.create(d, recursive = T)
}
})
# get the cells -----------------------------------------------------------
cells = rondas::get_cells(4*4*4)
cells
cells[[3]]
hawaii = list(y=19.8,x=-155.8)
map(cells, function(c){
return(hawaii$y > c$south &&
hawaii$y < c$north &&
hawaii$x > c$west &&
hawaii$x < c$east)
})
i = 4
# safe version of queriying data ------------------------------------------
safe_fromJSON = purrr::safely(fromJSON)
# get data ----------------------------------------------------------------
regions_data = vector("list", length(length(cells)))
# cell
cell = cells[[i]]
cell
#url
url = glue("https://services.surfline.com/kbyg/mapview?south={cell$south}&west={cell$west}&north={cell$north}&east={cell$east}")
# raw data
raw_data = safe_fromJSON(url)
raw_data
if(!is.null(raw_data$error)){
next
}
raw_data = raw_data$result
# formt raw mapview data
formatted_data = rondas::format_raw_mapview_data(raw_data)
# regions data
regions_data[[i]] = formatted_data

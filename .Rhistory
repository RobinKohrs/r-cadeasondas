# get the cells -----------------------------------------------------------
cells = rondas::get_cells(4*4*4)
# safe version of queriying data ------------------------------------------
safe_fromJSON = purrr::safely(fromJSON)
# get data ----------------------------------------------------------------
regions_data = vector("list", length(length(cells)))
i = 4
# cell
cell = cells[[i]]
#url
url = glue("https://services.surfline.com/kbyg/mapview?south={cell$south}&west={cell$west}&north={cell$north}&east={cell$east}")
# raw data
raw_data = safe_fromJSON(url)
raw_data
if(!is.null(raw_data$error)){
next
}
raw_data = raw_data$result
raw_data
# formt raw mapview data
formatted_data = rondas::format_raw_mapview_data(raw_data)
formatted_data
# regions data
regions_data[[i]] = formatted_data
regions_data
# filter out nas ----------------------------------------------------------
regions_data = regions_data[!is.na(regions_data)]
# bind --------------------------------------------------------------------
all = bind_rows(regions_data) %>%
select(-where(is.list))
all
# create path for the raw data ---------------------------------------------
time_id = format(Sys.time(), "%Y_%m_%d_%H")
op = glue("{dirs$raw_data}/{time_id}.Rds")
print(paste0("op: ", op))
if(file.exists(op)){
unlink(op)
}
op
per_spot_files = dir(dirs$per_spot_raw, "*")
per_sop
per_spot_files
op
devtools::install()
devtools::install()
devtools::install()
devtools::install()
cronR::cron_rstudioaddin()
cronR::cron_rstudioaddin()
cronR::cron_rstudioaddin()
# output base dir --------------------------------------------------------------
base_output_dir = "~/projects/personal/r/2023/rondas/data_processed/"
# output base dir --------------------------------------------------------------
base_output_dir = "~/projects/personal/r/2023/rondas/data_processed/"
if(!dir.exists(base_output_dir)){
dir.create(base_output_dir, recursive = T)
}
devtools::load_all()
base_output_dir = here("./data_processed/")
library(here)
library(dplyr)
library(tidyverse)
base_output_dir = here("./data_processed/")
# make daily data  ------------------------------------------------
# should include wave height, temperature, rating,...
# this is one csv with all 8k spots per day...
# keep getting more csvs
daily_data_dir = here(base_output_dir, "daily_data")
dir = daily_data_dir
dir
# dirs for the final data -------------------------------------------------
base_dir = "~/projects/personal/r/2023/rondas/data_downloaded/surfdata/"
dirs =
list(
raw_data = here(base_dir, "per_day"),
per_spot_raw = here(base_dir, "per_spot"),
per_spot_clean = here(base_dir, "per_spot_clean")
)
dirs
# dirs for the final data -------------------------------------------------
base_dir = "~/projects/personal/r/2023/rondas/"
dirs =
list(
raw_data = here(base_dir, "data_downloaded/surfdata/per_day"),
per_spot_raw = here(base_dir, "data_downloaded/surfdata/per_spot")
)
dirs
# dirs for the final data -------------------------------------------------
base_dir = "~/projects/personal/r/2023/rondas/"
dirs =
list(
raw_data = here(base_dir, "data_downloaded/surfdata/per_day"),
preprocessed_data = here(base_dir, "data_preprocessed")
# per_spot_raw = here(base_dir, "data_downloaded/surfdata/per_spot")
)
dirs
# dirs for the final data -------------------------------------------------
base_dir = "~/projects/personal/r/2023/rondas"
dirs =
list(
raw_data = here(base_dir, "data_downloaded/surfdata/per_day"),
preprocessed_data = here(base_dir, "data_preprocessed")
# per_spot_raw = here(base_dir, "data_downloaded/surfdata/per_spot")
)
dirs
walk(dirs, function(d) {
if (!dir.exists(d)) {
dir.create(d, recursive = T)
}
})
dirs
# input files
raw_data_files = here(dirs$raw_data, ".*\\.Rds", full.name=T)
# input files
raw_data_files = here(dirs$raw_data, ".*\\.Rds", full.names=T)
# input files
raw_data_files = dir(dirs$raw_data, ".*\\.Rds", full.names=T)
raw_data_files
# input files
input_dir = dirs$raw_data
# output
output_dir = here(dirs$preprocessed_data, "historical_per_spot")
output_dir
dir.create(output_dir)
# files in output_dir
output_files = dir(output_dir, ".*\\.csv", full.names=T)
output_files
output_dir
dir_data_raw = dirs$raw_data
# output dir for data per downloaded timestamp per spot
output_dir = here(dir_raw_data, "per_spot")
# output dir for data per downloaded timestamp per spot
output_dir = here(dir_data_raw, "per_spot")
output_dir
# files that are there already
dir(output_dir)
dir_data_raw
dirs$raw_data
# first summarise each timestamp data (not for github)
op_dir_per_timestamp = here(basename(dirs$raw_data), "per_spot")
op_dir_per_timestamp
# first summarise each timestamp data (not for github)
dir_per_timestamp = here(basename(dirs$raw_data), "per_spot")
dir_data_timestamp
# first summarise each timestamp data (not for github)
dir_per_timestamp = here(basename(dirs$raw_data), "per_spot")
# first summarise each timestamp data (not for github)
dir_data_timestamp = here(basename(dirs$raw_data), "per_spot")
dir_data_timestamp
# files that are there already
dir(dir_data_timestamp)
# first summarise each timestamp data (not for github)
dir_data_timestamp = here(basename(dirs$raw_data), "per_spot")
dir_data_timestamp
# first summarise each timestamp data (not for github)
dir_data_timestamp = here(dirname(dirs$raw_data), "per_spot")
dir_data_timestamp
# files that are there already
dir(dir_data_timestamp)
# files that are there already
per_spot_timestamp_files = dir(dir_data_timestamp, ".*\\.csv")
per_spot_timestamp_files
# per timestamp file
per_timestamp_files = dir(dir_data_raw, ".*\\.Rds", full.names=T)
per_timestamp_files
per_timestamp_files
# find the last raw data file
per_timestamp_files[length(per_timestamp_files)]
# per timestamp file
per_timestamp_files = dir(dir_data_raw, ".*\\.Rds", full.names=T)
# if summarise each timestamp for each spot for the first time
if(length(per_spot_timestamp_files) == 0){
files = per_timestamp_files
}else{
# find the last raw data file
files = per_timestamp_files[length(per_timestamp_files)]
}
files
seq_along(files)
j =
j = 1
f = files[[j]]
# date
bn = basename(f)
date_str = str_extract(bn, "\\d+_\\d+_\\d+_\\d+")
# data
d = readRDS(f)
per_id = d %>%
split(.$`_id`)
per_id$5842041f4e65fad6a77087f9
devtools::load_all()
devtools::document()
devtools::load_all()
devtools::load_all()
library(tidyverse)
library(here)
library(glue)
library(sf)
library(rajudas)
library(jsonlite)
library(lubridate)
library(devtools)
# dirs for the data -------------------------------------------------
base_dir = "~/projects/personal/r/2023/rondas"
dirs =
list(
raw_data = here(base_dir, "data_downloaded/surfdata/per_day"),
preprocessed_data = here(base_dir, "data_preprocessed")
# per_spot_raw = here(base_dir, "data_downloaded/surfdata/per_spot")
)
walk(dirs, function(d) {
if (!dir.exists(d)) {
dir.create(d, recursive = T)
}
})
# get the cells -----------------------------------------------------------
cells = rondas::get_cells(4*4*4)
# safe version of queriying data ------------------------------------------
safe_fromJSON = purrr::safely(fromJSON)
# get raw data ----------------------------------------------------------------
regions_data = vector("list", length(length(cells)))
for(i in seq_along(cells)){
# cell
cell = cells[[i]]
#url
url = glue("https://services.surfline.com/kbyg/mapview?south={cell$south}&west={cell$west}&north={cell$north}&east={cell$east}")
# raw data
raw_data = safe_fromJSON(url)
if(!is.null(raw_data$error)){
next
}
raw_data = raw_data$result
# formt raw mapview data
formatted_data = rondas::format_raw_mapview_data(raw_data)
# regions data
regions_data[[i]] = formatted_data
}
# filter out nas
regions_data = regions_data[!is.na(regions_data)]
regions_data
# bind together to make on big chunky df
all = bind_rows(regions_data) %>%
select(-where(is.list))
# create path for the raw data
time_id = format(Sys.time(), "%Y_%m_%d_%H")
time_id
op = glue("{dirs$raw_data}/{time_id}.Rds")
print(paste0("op: ", op))
if(file.exists(op)){
unlink(op)
}
if(!dir.exists(dirname(op))){
dir.create(dirname(op), recursive = T)
}
op
saveRDS(all, op)
dirs
# first summarise each timestamp data (not for github)
dir_data_timestamp = here(dirname(dirs$raw_data), "per_spot")
dir_data_timestamp
dir_data_raw = dirs$raw_data
dir_data_timestamp=dir_data_timestamp
# files that are there already
per_spot_timestamp_files = dir(dir_data_timestamp, ".*\\.csv")
per_spot_timestamp_files
# per timestamp file
per_timestamp_files = dir(dir_data_raw, ".*\\.Rds", full.names=T)
# if summarise each timestamp for each spot for the first time
if(length(per_spot_timestamp_files) == 0){
files = per_timestamp_files
}else{
# find the last raw data file
files = per_timestamp_files[length(per_timestamp_files)]
}
files
# for each day
walk(seq_along(files), function(j){
f = files[[j]]
# date
bn = basename(f)
date_str = str_extract(bn, "\\d+_\\d+_\\d+_\\d+")
# data
d = readRDS(f)
per_id = d %>%
split(.$`_id`)
walk(seq_along(per_id), function(i){
# cat(glue("{j}: {i}\r"))
spot = per_id[[i]]
id = spot$`_id`[[1]]
name = spot$name[[1]] %>% janitor::make_clean_names()
name = glue("{name}_{id}")
path_spot = glue("{output_dir}/{name}.csv")
spot[["time"]] = date_str
d_spot = spot %>%
select(time, lat, lon, name, conditions__value, rating__key, rating__value, matches("^surf|waveHeight|wind__|waterTemp|swells__|weather__"))
d_spot_one_swell = d_spot %>%
filter(case_when(
all(is.na(swells__event)) ~ swells__swell_nr == 1,
.default = swells__event == min(swells__event, na.rm = T)
))
data.table::fwrite(d_spot_one_swell, path_spot, append = T)
})
})
dir_data_timestamp
# for each day
walk(seq_along(files), function(j){
f = files[[j]]
# date
bn = basename(f)
date_str = str_extract(bn, "\\d+_\\d+_\\d+_\\d+")
# data
d = readRDS(f)
per_id = d %>%
split(.$`_id`)
walk(seq_along(per_id), function(i){
# cat(glue("{j}: {i}\r"))
spot = per_id[[i]]
id = spot$`_id`[[1]]
name = spot$name[[1]] %>% janitor::make_clean_names()
name = glue("{name}_{id}")
path_spot = glue("{dir_data_timestamp}/{name}.csv")
spot[["time"]] = date_str
d_spot = spot %>%
select(time, lat, lon, name, conditions__value, rating__key, rating__value, matches("^surf|waveHeight|wind__|waterTemp|swells__|weather__"))
d_spot_one_swell = d_spot %>%
filter(case_when(
all(is.na(swells__event)) ~ swells__swell_nr == 1,
.default = swells__event == min(swells__event, na.rm = T)
))
data.table::fwrite(d_spot_one_swell, path_spot, append = T)
})
})
cronR::cron_rstudioaddin()
devtools::install()
devtools::install(quick = T)
cronR::cron_rstudioaddin()
# for the log -------------------------------------------------------------
t = Sys.time() %>% str_sub(1,19)
print(glue("Run at: {t}"))
# dirs for the data -------------------------------------------------
base_dir = "~/projects/personal/r/2023/rondas"
dirs =
list(
raw_data = here(base_dir, "data_downloaded/surfdata/per_day"),
preprocessed_data = here(base_dir, "data_preprocessed")
# per_spot_raw = here(base_dir, "data_downloaded/surfdata/per_spot")
)
suppressMessages(library(tidyverse))
suppressMessages(library(here))
library(glue)
suppressMessages(library(sf))
library(rajudas)
library(jsonlite)
suppressMessages(library(lubridate))
suppressMessages(library(devtools))
library(rondas)
# for the log -------------------------------------------------------------
t = Sys.time() %>% str_sub(1,19)
print(glue("Run at: {t}"))
# dirs for the data -------------------------------------------------
base_dir = "~/projects/personal/r/2023/rondas"
dirs =
list(
raw_data = here(base_dir, "data_downloaded/surfdata/per_day"),
preprocessed_data = here(base_dir, "data_preprocessed")
# per_spot_raw = here(base_dir, "data_downloaded/surfdata/per_spot")
)
raw_data_timestamp = dirs$raw_data
daily_data_dir = here(dirs$preprocessed_data, "daily_data")
daily_data_dir
# files -------------------------------------------------------------------
raw_files = dir(raw_data_timestamp, ".*\\.Rds", full.names = T)
# find all the present days -----------------------------------------------
dates = str_sub(basename(raw_files), 1, 10)
dates_unique = unique(dates)
d = dates_unique[[10]]
dang::lsos()
rm(list=ls())
raw_data_timestamp = dirs$raw_data
# for the log -------------------------------------------------------------
t = Sys.time() %>% str_sub(1,19)
print(glue("Run at: {t}"))
# dirs for the data -------------------------------------------------
base_dir = "~/projects/personal/r/2023/rondas"
dirs =
list(
raw_data = here(base_dir, "data_downloaded/surfdata/per_day"),
preprocessed_data = here(base_dir, "data_preprocessed")
# per_spot_raw = here(base_dir, "data_downloaded/surfdata/per_spot")
)
walk(dirs, function(d) {
if (!dir.exists(d)) {
dir.create(d, recursive = T)
}
})
raw_data_timestamp = dirs$raw_data
daily_data_dir = here(dirs$preprocessed_data, "daily_data")
# files -------------------------------------------------------------------
raw_files = dir(raw_data_timestamp, ".*\\.Rds", full.names = T)
# find all the present days -----------------------------------------------
dates = str_sub(basename(raw_files), 1, 10)
dates_unique = unique(dates)
d = dates_unique[[10]]
d
op_that_date = makePath(here(daily_data_dir, "data", glue("{d}.csv")))
if(file.exists(op_that_date)) return()
# files for that date with all timestampts
files_that_date = raw_files[str_which(dates, d)]
files_that_date
dang::lsos()
# read all the data
data_one_day = map(files_that_date, function(f){
bn = basename(f) %>% tools::file_path_sans_ext()
d = readRDS(f) %>%
mutate(timestamp = bn)
}) %>% bind_rows
data_one_day
dang::lsos()
single_swell_per_spot_timestamp = data_one_day %>%
group_by(`_id`, timestamp) %>%
distinct(.keep_all = T) %>%
filter(case_when(
all(is.na(swells__event)) ~ swells__swell_nr == 1,
.default = swells__event == min(swells__event, na.rm = T)
))
rm(data_one_day)
data_per_day_spot = single_swell_per_spot_timestamp %>%
mutate(date = str_sub(timestamp, 1, 10)) %>%
group_by(`_id`, date) %>%
summarise(
lat = first(lat),
lon = first(lon),
name = as.character(first(name)),
daily_mean_swell_rating = mean(rating__value, na.rm = T),
daily_mean_wave_height_min = mean(surf__min, na.rm = T),
daily_mean_wave_height_max = mean(surf__max, na.rm = T),
daily_mean_water_temperature_max = mean(waterTemp__max, na.rm =
T),
daily_mean_water_temperature_min = mean(waterTemp__min, na.rm =
T),
daily_mean_water_temperature_max = mean(waterTemp__max, na.rm =
T),
weather_per_timestamp = paste0(weather__condition, collapse = ","),
wind_per_timestamp = paste0(wind__directionType, collapse = ","),
n_samples_per_day = n()
)
data_per_day_spot
rm(single_swell_per_spot_timestamp)
dang::lsos()
op_that_date
write_csv(data_per_day_spot, op_that_date)
# for each date get the data for each spot --------------------------------
walk(dates_unique, function(d){
op_that_date = makePath(here(daily_data_dir, "data", glue("{d}.csv")))
if(file.exists(op_that_date)) return()
# files for that date with all timestampts
files_that_date = raw_files[str_which(dates, d)]
# read all the data
data_one_day = map(files_that_date, function(f){
bn = basename(f) %>% tools::file_path_sans_ext()
d = readRDS(f) %>%
mutate(timestamp = bn)
}) %>% bind_rows
single_swell_per_spot_timestamp = data_one_day %>%
group_by(`_id`, timestamp) %>%
distinct(.keep_all = T) %>%
filter(case_when(
all(is.na(swells__event)) ~ swells__swell_nr == 1,
.default = swells__event == min(swells__event, na.rm = T)
))
rm(data_one_day)
data_per_day_spot = single_swell_per_spot_timestamp %>%
mutate(date = str_sub(timestamp, 1, 10)) %>%
group_by(`_id`, date) %>%
summarise(
lat = first(lat),
lon = first(lon),
name = as.character(first(name)),
daily_mean_swell_rating = mean(rating__value, na.rm = T),
daily_mean_wave_height_min = mean(surf__min, na.rm = T),
daily_mean_wave_height_max = mean(surf__max, na.rm = T),
daily_mean_water_temperature_max = mean(waterTemp__max, na.rm =
T),
daily_mean_water_temperature_min = mean(waterTemp__min, na.rm =
T),
daily_mean_water_temperature_max = mean(waterTemp__max, na.rm =
T),
weather_per_timestamp = paste0(weather__condition, collapse = ","),
wind_per_timestamp = paste0(wind__directionType, collapse = ","),
n_samples_per_day = n()
)
rm(single_swell_per_spot_timestamp)
write_csv(data_per_day_spot, op_that_date)
})
dang::lsos()
# file with all the dates available ---------------------------------------
op_index_days = makePath(daily_data_dir, "index_days.csv")
# file with all the dates available ---------------------------------------
op_index_days = makePath(here(daily_data_dir, "index_days.csv"))
op_index_days
dates_unique
toJSON(dates_unique)
dates_unqie_json = jsonlite::toJSON(dates_unique)
devtools::document()
# file with all the dates available ---------------------------------------
op_index_days = makePath(here(daily_data_dir, "index_days.csv"))
dates_unique_json = jsonlite::toJSON(dates_unique)
write(dates_unqie_json, op_index_days)
# file with all the dates available ---------------------------------------
op_index_days = makePath(here(daily_data_dir, "index_days.json"))
dates_unique_json = jsonlite::toJSON(dates_unique)
write(dates_unqie_json, op_index_days)
